{
  "version": 1,
  "description": "Cursor-native hook port of Claude Code hooks.json. Shared scripts; event names mapped.",
  "hooks": {
    "postToolUse": [
      {
        "command": "./\"./scripts/okf-hook-validate.sh\"",
        "matcher": "apply_patch|Write|Edit|MultiEdit",
        "timeout": 45
      }
    ],
    "afterFileEdit": [
      {
        "command": "./\"./scripts/okf-hook-validate.sh\"",
        "timeout": 45
      }
    ]
  }
}
